document.addEventListener('DOMContentLoaded',()=>{const root=document.querySelector('[data-map-fields]');if(!root)return;const select=root.querySelector('[data-map-select]'),map=root.querySelector('[data-position-map]'),image=map.querySelector('img'),pin=map.querySelector('span'),x=root.querySelector('[data-position-x]'),y=root.querySelector('[data-position-y]'),axis=root.querySelector('.map-axis-fields');const place=()=>{pin.style.left=`${Math.max(0,Math.min(100,Number(x.value)||0))}%`;pin.style.top=`${Math.max(0,Math.min(100,Number(y.value)||0))}%`};const render=()=>{const option=select.selectedOptions[0],src=option?.dataset.image||'',outside=select.value==='outside'||!src;map.hidden=outside;axis.hidden=outside;x.disabled=outside;y.disabled=outside;if(!outside){image.src=src;place()}};select.addEventListener('change',render);x.addEventListener('input',place);y.addEventListener('input',place);map.addEventListener('click',event=>{const rect=map.getBoundingClientRect();x.value=Math.max(0,Math.min(100,(event.clientX-rect.left)/rect.width*100)).toFixed(1);y.value=Math.max(0,Math.min(100,(event.clientY-rect.top)/rect.height*100)).toFixed(1);place()});render()});
