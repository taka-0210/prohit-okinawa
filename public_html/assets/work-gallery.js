document.addEventListener('DOMContentLoaded',()=>{const dialog=document.createElement('dialog');dialog.className='work-lightbox';dialog.innerHTML='<div class="lightbox-inner"><div class="lightbox-head"><h2></h2><button type="button" data-close>閉じる ×</button></div><div class="lightbox-image"><img alt=""></div><div class="lightbox-controls"><button type="button" data-prev>← 前へ</button><span></span><button type="button" data-next>次へ →</button></div></div>';document.body.append(dialog);let images=[],index=0;const img=dialog.querySelector('img'),count=dialog.querySelector('.lightbox-controls span'),render=()=>{if(!images.length)return;index=(index+images.length)%images.length;img.src=images[index];count.textContent=`${index+1} / ${images.length}`};document.querySelectorAll('.work-gallery-trigger').forEach(button=>button.addEventListener('click',()=>{try{images=JSON.parse(button.dataset.images||'[]')}catch{images=[]}index=0;dialog.querySelector('h2').textContent=button.dataset.title||'';render();dialog.showModal()}));dialog.querySelector('[data-close]').addEventListener('click',()=>dialog.close());dialog.querySelector('[data-prev]').addEventListener('click',()=>{index--;render()});dialog.querySelector('[data-next]').addEventListener('click',()=>{index++;render()});dialog.addEventListener('click',event=>{if(event.target===dialog)dialog.close()});document.addEventListener('keydown',event=>{if(!dialog.open)return;if(event.key==='ArrowLeft'){index--;render()}if(event.key==='ArrowRight'){index++;render()}})});
